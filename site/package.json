{
  "name": "gotthard-traffic-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "build:fast": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "fetch": "node scripts/fetch-data.mjs",
    "fetch:watch": "node scripts/fetch-data.mjs --watch",
    "fetch:history": "node scripts/fetch-yearly-history.mjs"
  },
  "dependencies": {
    "astro": "^7.0.6"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@types/node": "^26.1.0",
    "playwright": "^1.61.1",
    "typescript": "^5.9.3"
  }
}
